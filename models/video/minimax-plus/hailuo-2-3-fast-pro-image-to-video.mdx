---
title: "Hailuo 2.3 Fast Pro — Image to Video"
api: "POST /v1/jobs/submit"
description: "Generate videos from images using MiniMax Plus Hailuo 2.3 Fast Pro model"
---

Animate a still image with MiniMax Plus Hailuo 2.3 Fast Pro — a 1080p image-to-video model that combines fast generation with pro-tier quality at a flat per-request cost.

| Property | Value |
|----------|-------|
| **Provider** | MiniMax Plus |
| **Model** | Hailuo 2.3 Fast Pro |
| **Capability** | Image to Video |
| **Base Cost** | 35,000 micro-cents/request ($0.035/request) |
| **Processing Time** | ~90 seconds |

## Request Body

<ParamField body="model" type="string" required placeholder="minimax-plus/hailuo-2.3-fast-pro/image-to-video">
  Model slug. Use `minimax-plus/hailuo-2.3-fast-pro/image-to-video` for image-to-video generation.
</ParamField>

<ParamField body="input" type="object" required>
  Input parameters for image-to-video generation.
  <Expandable title="properties" defaultOpen>
    <ParamField body="image_url" type="string" required>
      URL of the input image to animate.
    </ParamField>
    <ParamField body="prompt" type="string">
      Text description guiding the animation (max 2500 characters).
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="webhookUrl" type="string">
  HTTPS URL to receive a webhook notification when the job completes or fails.
</ParamField>

## Pricing

**Base cost:** 35,000 micro-cents per request ($0.035/request)

<Tip>
**Default cost:** **35,000 micro-cents ($0.035)** per request — flat pricing regardless of duration.
</Tip>

## Response

<ResponseField name="jobId" type="string">
  Unique identifier for the submitted job.
</ResponseField>

<ResponseField name="status" type="string">
  Initial job status. Always `"pending"` on successful submission.
</ResponseField>

<ResponseField name="estimatedCompletionTime" type="string">
  ISO 8601 timestamp of the estimated completion time.
</ResponseField>

<ResponseField name="costMicroCents" type="number">
  The cost of the job in micro-cents.
</ResponseField>

## Code Examples

<CodeGroup>

```bash cURL
curl -X POST https://api.muvi.video/v1/jobs/submit \
  -H "Authorization: Bearer $PIXELBYTE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "minimax-plus/hailuo-2.3-fast-pro/image-to-video",
    "input": {
      "image_url": "https://example.com/image.jpg",
      "prompt": "Cinematic camera push-in as the subject turns toward the light"
    }
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.muvi.video/v1/jobs/submit",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json"
    },
    json={
        "model": "minimax-plus/hailuo-2.3-fast-pro/image-to-video",
        "input": {
            "image_url": "https://example.com/image.jpg",
            "prompt": "Cinematic camera push-in as the subject turns toward the light"
        }
    }
)

data = response.json()
print(data)
```

```javascript JavaScript
const response = await fetch("https://api.muvi.video/v1/jobs/submit", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    model: "minimax-plus/hailuo-2.3-fast-pro/image-to-video",
    input: {
      image_url: "https://example.com/image.jpg",
      prompt: "Cinematic camera push-in as the subject turns toward the light"
    }
  })
});

const data = await response.json();
console.log(data);
```

</CodeGroup>
