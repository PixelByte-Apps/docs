---
title: "Kling 3.0 Standard"
api: "POST /v1/jobs/submit"
description: "Generate videos with Kling 3.0 Standard — supports text-to-video, image-to-video, and start-end-frame-to-video"
---

Generate high-quality videos with Kling 3.0 Standard. Supports text-to-video, image-to-video, and start-end-frame-to-video capabilities with optional audio generation.

| Property | Value |
|----------|-------|
| **Provider** | Kling |
| **Model** | Kling 3.0 Standard |
| **Capabilities** | Text to Video, Image to Video, Start-End Frame to Video |
| **Base Cost** | 84,000 micro-cents/second ($0.084/sec) |
| **Processing Time** | ~240 seconds |

## Model Slugs

| Capability | Slug |
|------------|------|
| Text to Video | `kling/kling-3.0-standard/text-to-video` |
| Image to Video | `kling/kling-3.0-standard/image-to-video` |
| Start-End Frame to Video | `kling/kling-3.0-standard/start-end-frame-to-video` |

## Request Body

<ParamField body="model" type="string" required placeholder="kling/kling-3.0-standard/text-to-video">
  Model slug. Use one of the slugs listed above based on the desired capability.
</ParamField>

<ParamField body="input" type="object" required>
  Input parameters for video generation.
  <Expandable title="properties" defaultOpen>
    <ParamField body="prompt" type="string" required>
      Text description of the video to generate (max 2500 characters).
    </ParamField>
    <ParamField body="image_url" type="string">
      URL of the input image. **Required** for `image-to-video`.
    </ParamField>
    <ParamField body="start_frame_url" type="string">
      URL of the start frame image. **Required** for `start-end-frame-to-video`.
    </ParamField>
    <ParamField body="end_frame_url" type="string">
      URL of the end frame image. **Required** for `start-end-frame-to-video`.
    </ParamField>
    <ParamField body="aspect_ratio" type="string">
      Video aspect ratio. Default: `16:9`. Options: `16:9`, `9:16`, `1:1`.
    </ParamField>
    <ParamField body="duration" type="string">
      Video duration in seconds. Default: `5`. Options: `5`, `10`, `15`.
    </ParamField>
    <ParamField body="has_sound" type="string">
      Enable audio generation. Default: `false`. Options: `true`, `false`.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="webhookUrl" type="string">
  HTTPS URL to receive a webhook notification when the job completes or fails.
</ParamField>

## Pricing

**Base cost:** 84,000 micro-cents per second ($0.084/sec)

```
finalCost = baseCost × duration × has_sound
```

| Factor | Option | Multiplier |
|--------|--------|-----------|
| **Duration** | `5` | 5x |
| | `10` | 10x |
| | `15` | 15x |
| **Sound** | `false` | 1x |
| | `true` | 1.5x |

<Tip>
**Default cost:** 5 seconds, no sound = 84,000 × 5 × 1 = **420,000 micro-cents ($0.42)**
</Tip>

## Response

<ResponseField name="jobId" type="string">
  Unique identifier for the submitted job.
</ResponseField>

<ResponseField name="status" type="string">
  Initial job status. Always `"pending"` on successful submission.
</ResponseField>

<ResponseField name="estimatedCompletionTime" type="string">
  ISO 8601 timestamp of the estimated completion time.
</ResponseField>

<ResponseField name="costMicroCents" type="number">
  The cost of the job in micro-cents.
</ResponseField>

## Code Examples

<CodeGroup>

```bash cURL
curl -X POST https://api.muvi.video/v1/jobs/submit \
  -H "Authorization: Bearer $PIXELBYTE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "kling/kling-3.0-standard/text-to-video",
    "input": {
      "prompt": "A golden retriever running through a sunlit meadow",
      "aspect_ratio": "16:9",
      "duration": "5",
      "has_sound": "true"
    }
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.muvi.video/v1/jobs/submit",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json"
    },
    json={
        "model": "kling/kling-3.0-standard/text-to-video",
        "input": {
            "prompt": "A golden retriever running through a sunlit meadow",
            "aspect_ratio": "16:9",
            "duration": "5",
            "has_sound": "true"
        }
    }
)

data = response.json()
print(data)
```

```javascript JavaScript
const response = await fetch("https://api.muvi.video/v1/jobs/submit", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    model: "kling/kling-3.0-standard/text-to-video",
    input: {
      prompt: "A golden retriever running through a sunlit meadow",
      aspect_ratio: "16:9",
      duration: "5",
      has_sound: "true"
    }
  })
});

const data = await response.json();
console.log(data);
```

</CodeGroup>
