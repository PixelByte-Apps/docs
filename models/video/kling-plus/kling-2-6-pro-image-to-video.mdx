---
title: "Kling 2.6 Pro — Image to Video"
api: "POST /v1/jobs/submit"
description: "Generate image to video using Kling 2.6 Pro via PixelByte API"
---

Animate a static image into a video using a text prompt with Kling 2.6 Pro. Supports multiple durations and prompt-guidance controls.

| Property | Value |
|----------|-------|
| **Provider** | Kling Plus |
| **Model** | Kling 2.6 Pro |
| **Capability** | Image to Video |
| **Base Cost** | 70,000 micro-cents/second ($0.07/second) |
| **Processing Time** | ~240 seconds |

## Request Body

<ParamField body="model" type="string" required placeholder="kling-plus/kling-2.6-pro/image-to-video">
  Model slug. Use `kling-plus/kling-2.6-pro/image-to-video` for image to video generation.
</ParamField>

<ParamField body="input" type="object" required>
  Input parameters for image to video generation.
  <Expandable title="properties" defaultOpen>
    <ParamField body="prompt" type="string">
      Text description of the video to generate. max 2500 characters.
    </ParamField>
    <ParamField body="image_url" type="string" required>
      URL of the input image to animate.
    </ParamField>
    <ParamField body="end_image" type="string">
      URL of ending image.
    </ParamField>
    <ParamField body="duration" type="string">
      Video duration in seconds. Options: `5`, `10`. Default: `5`.
    </ParamField>
    <ParamField body="has_sound" type="string">
      Enable audio generation. Options: `false`, `true`. Default: `true`.
    </ParamField>
    <ParamField body="cfg_scale" type="number">
      Prompt adherence strength. min 0, max 1. Default: `0.5`.
    </ParamField>
    <ParamField body="negative_prompt" type="string">
      Negative prompt. max 1000 characters.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="webhookUrl" type="string">
  HTTPS URL to receive a webhook notification when the job completes or fails.
</ParamField>

## Pricing

**Base cost:** 70,000 micro-cents per second ($0.07/second)

```
finalCost = baseCost × has_sound × duration
```

| Factor | Option | Multiplier |
|--------|--------|-----------|
| **Has Sound** | `true` | 2x |
|  | `false` | 1x |
| **Duration** | `5` | 5x |
|  | `10` | 10x |

<Tip>
**Default cost** (has_sound=`true`, duration=`5`): 70,000 × 2 × 5 = **700,000 micro-cents ($0.7)**
</Tip>

## Response

<ResponseField name="jobId" type="string">
  Unique identifier for the submitted job.
</ResponseField>

<ResponseField name="status" type="string">
  Initial job status. Always `"pending"` on successful submission.
</ResponseField>

<ResponseField name="estimatedCompletionTime" type="string">
  ISO 8601 timestamp of the estimated completion time.
</ResponseField>

<ResponseField name="costMicroCents" type="number">
  The cost of the job in micro-cents.
</ResponseField>

## Code Examples

<CodeGroup>

```bash cURL
curl -X POST https://api.muvi.video/v1/jobs/submit \
  -H "Authorization: Bearer $PIXELBYTE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "kling-plus/kling-2.6-pro/image-to-video",
    "input": {
      "image_url": "https://example.com/image.jpg",
      "prompt": "The dog starts running and jumps over a wooden fence in slow motion.",
      "duration": "5",
      "has_sound": "true"
    }
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.muvi.video/v1/jobs/submit",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json"
    },
    json={
        "model": "kling-plus/kling-2.6-pro/image-to-video",
        "input": {
            "image_url": "https://example.com/image.jpg",
            "prompt": "The dog starts running and jumps over a wooden fence in slow motion.",
            "duration": "5",
            "has_sound": "true"
        }
    }
)

data = response.json()
print(data)
```

```javascript JavaScript
const response = await fetch("https://api.muvi.video/v1/jobs/submit", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    model: "kling-plus/kling-2.6-pro/image-to-video",
    input: {
      image_url: "https://example.com/image.jpg",
      prompt: "The dog starts running and jumps over a wooden fence in slow motion.",
      duration: "5",
      has_sound: "true"
    }
  })
});

const data = await response.json();
console.log(data);
```

</CodeGroup>
