---
title: "Kling O1 Pro — Reference to Video"
api: "POST /v1/jobs/submit"
description: "Generate videos with the Kling O1 Pro model using reference to video."
---

Generate a video conditioned on reference images and/or a reference video using the Kling O1 Pro model.

| Property | Value |
|----------|-------|
| **Provider** | Kling Plus |
| **Model** | Kling O1 Pro |
| **Capability** | Reference to Video |
| **Base Cost** | 70,000 micro-cents/second ($0.070/sec) |
| **Processing Time** | ~240 seconds |

## Request Body

<ParamField body="model" type="string" required placeholder="kling-plus/kling-o1-pro/reference-to-video">
  Model slug. Use `kling-plus/kling-o1-pro/reference-to-video` for reference to video generation.
</ParamField>

<ParamField body="input" type="object" required>
  Input parameters for reference to video generation.
  <Expandable title="properties" defaultOpen>
    <ParamField body="prompt" type="string" required>
      Text prompt describing the desired output (max 2500 characters).
    </ParamField>
    <ParamField body="video_url" type="string">
      Reference video URL.
    </ParamField>
    <ParamField body="images" type="array">
      Reference images (≤4 with video, ≤7 without).
    </ParamField>
    <ParamField body="aspect_ratio" type="string">
      Video aspect ratio. Default: `16:9`. Options: `16:9`, `9:16`, `1:1`.
    </ParamField>
    <ParamField body="duration" type="string">
      Video duration in seconds. Default: `5`. Options: `3`, `4`, `5`, `6`, `7`, `8`, `9`, `10`.
    </ParamField>
    <ParamField body="keep_original_sound" type="boolean">
      Keep the audio track from the source video. Default: `true`.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="webhookUrl" type="string">
  HTTPS URL to receive a webhook notification when the job completes or fails.
</ParamField>

## Pricing

**Base cost:** 70,000 micro-cents per second ($0.070/sec)

```
finalCost = baseCost × duration
```

| Factor | Option | Multiplier |
|--------|--------|-----------|
| **Duration** | `3` | 3x |
|  | `4` | 4x |
|  | `5` | 5x |
|  | `6` | 6x |
|  | `7` | 7x |
|  | `8` | 8x |
|  | `9` | 9x |
|  | `10` | 10x |

<Tip>
**Default cost:** 5 seconds = 70,000 × 5 = **350,000 micro-cents ($0.350)**
</Tip>

## Response

<ResponseField name="jobId" type="string">
  Unique identifier for the submitted job.
</ResponseField>

<ResponseField name="status" type="string">
  Initial job status. Always `"pending"` on successful submission.
</ResponseField>

<ResponseField name="estimatedCompletionTime" type="string">
  ISO 8601 timestamp of the estimated completion time.
</ResponseField>

<ResponseField name="costMicroCents" type="number">
  The cost of the job in micro-cents.
</ResponseField>

## Code Examples

<CodeGroup>

```bash cURL
curl -X POST https://api.muvi.video/v1/jobs/submit \
  -H "Authorization: Bearer $PIXELBYTE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "kling-plus/kling-o1-pro/reference-to-video",
    "input": {
      "prompt": "Recreate the reference scene with warmer lighting and a slow dolly-in",
      "video_url": "https://example.com/video.mp4",
      "aspect_ratio": "16:9",
      "duration": "5"
    }
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.muvi.video/v1/jobs/submit",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json"
    },
    json={
        "model": "kling-plus/kling-o1-pro/reference-to-video",
        "input": {
            "prompt": "Recreate the reference scene with warmer lighting and a slow dolly-in",
            "video_url": "https://example.com/video.mp4",
            "aspect_ratio": "16:9",
            "duration": "5"
        }
    }
)

data = response.json()
print(data)
```

```javascript JavaScript
const response = await fetch("https://api.muvi.video/v1/jobs/submit", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    model: "kling-plus/kling-o1-pro/reference-to-video",
    input: {
      prompt: "Recreate the reference scene with warmer lighting and a slow dolly-in",
      video_url: "https://example.com/video.mp4",
      aspect_ratio: "16:9",
      duration: "5"
    }
  })
});

const data = await response.json();
console.log(data);
```

</CodeGroup>
