---
title: "Kling O1 Standard — Text to Video"
api: "POST /v1/jobs/submit"
description: "Generate videos with the Kling O1 Standard model using text to video."
---

Generate a video purely from a text prompt with the Kling O1 Standard model. Choose duration and aspect ratio to fit your scene.

| Property | Value |
|----------|-------|
| **Provider** | Kling Plus |
| **Model** | Kling O1 Standard |
| **Capability** | Text to Video |
| **Base Cost** | 50,000 micro-cents/second ($0.050/sec) |
| **Processing Time** | ~240 seconds |

## Request Body

<ParamField body="model" type="string" required placeholder="kling-plus/kling-o1-standard/text-to-video">
  Model slug. Use `kling-plus/kling-o1-standard/text-to-video` for text to video generation.
</ParamField>

<ParamField body="input" type="object" required>
  Input parameters for text to video generation.
  <Expandable title="properties" defaultOpen>
    <ParamField body="prompt" type="string" required>
      Text prompt describing the desired output (max 2500 characters).
    </ParamField>
    <ParamField body="aspect_ratio" type="string">
      Video aspect ratio. Default: `16:9`. Options: `16:9`, `9:16`, `1:1`.
    </ParamField>
    <ParamField body="duration" type="string">
      Video duration in seconds. Default: `5`. Options: `5`, `10`.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="webhookUrl" type="string">
  HTTPS URL to receive a webhook notification when the job completes or fails.
</ParamField>

## Pricing

**Base cost:** 50,000 micro-cents per second ($0.050/sec)

```
finalCost = baseCost × duration
```

| Factor | Option | Multiplier |
|--------|--------|-----------|
| **Duration** | `5` | 5x |
|  | `10` | 10x |

<Tip>
**Default cost:** 5 seconds = 50,000 × 5 = **250,000 micro-cents ($0.250)**
</Tip>

## Response

<ResponseField name="jobId" type="string">
  Unique identifier for the submitted job.
</ResponseField>

<ResponseField name="status" type="string">
  Initial job status. Always `"pending"` on successful submission.
</ResponseField>

<ResponseField name="estimatedCompletionTime" type="string">
  ISO 8601 timestamp of the estimated completion time.
</ResponseField>

<ResponseField name="costMicroCents" type="number">
  The cost of the job in micro-cents.
</ResponseField>

## Code Examples

<CodeGroup>

```bash cURL
curl -X POST https://api.muvi.video/v1/jobs/submit \
  -H "Authorization: Bearer $PIXELBYTE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "kling-plus/kling-o1-standard/text-to-video",
    "input": {
      "prompt": "A neon-lit Tokyo street at night with rain reflecting the storefront signs",
      "aspect_ratio": "16:9",
      "duration": "5"
    }
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.muvi.video/v1/jobs/submit",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json"
    },
    json={
        "model": "kling-plus/kling-o1-standard/text-to-video",
        "input": {
            "prompt": "A neon-lit Tokyo street at night with rain reflecting the storefront signs",
            "aspect_ratio": "16:9",
            "duration": "5"
        }
    }
)

data = response.json()
print(data)
```

```javascript JavaScript
const response = await fetch("https://api.muvi.video/v1/jobs/submit", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    model: "kling-plus/kling-o1-standard/text-to-video",
    input: {
      prompt: "A neon-lit Tokyo street at night with rain reflecting the storefront signs",
      aspect_ratio: "16:9",
      duration: "5"
    }
  })
});

const data = await response.json();
console.log(data);
```

</CodeGroup>
