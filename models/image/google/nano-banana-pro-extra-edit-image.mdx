---
title: "Nano Banana Pro (Extra) — Image to Image / Edit"
api: "POST /v1/jobs/submit"
description: "Edit and transform images with multi-image prompts using Google's Nano Banana Pro (Extra) model via PixelByte API"
---

Edit, transform, or compose images by providing one or more input images and a text prompt with Google's Nano Banana Pro (Extra) model. Supports up to 4K resolution output.

| Property | Value |
|----------|-------|
| **Provider** | Google |
| **Model** | Nano Banana Pro (Extra) |
| **Capability** | Image to Image / Edit |
| **Base Cost** | 134,000 micro-cents/request ($0.134/request) |
| **Processing Time** | ~30 seconds |

## Request Body

<ParamField body="model" type="string" required placeholder="google/nano-banana-pro-extra/edit-image">
  Model slug. Use `google/nano-banana-pro-extra/edit-image` for image editing.
</ParamField>

<ParamField body="input" type="object" required>
  Input parameters for image editing.
  <Expandable title="properties" defaultOpen>
    <ParamField body="prompt" type="string" required>
      Text description of the desired edit or transformation.
    </ParamField>
    <ParamField body="image_urls" type="array" required>
      Array of input image URLs to edit or compose. At least one image is required.
    </ParamField>
    <ParamField body="aspect_ratio" type="string">
      Output aspect ratio. Default: `9:16`. Options: `9:16`, `16:9`, `1:1`, `2:3`, `3:2`, `21:9`, `3:4`, `4:3`, `4:5`, `5:4`.
    </ParamField>
    <ParamField body="resolution" type="string">
      Output resolution. Default: `1k`. Options: `1k`, `2k`, `4k`. Affects pricing.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="webhookUrl" type="string">
  HTTPS URL to receive a webhook notification when the job completes or fails.
</ParamField>

## Pricing

**Base cost:** 134,000 micro-cents per request ($0.134/request)

```
finalCost = baseCost × resolution
```

| Factor | Option | Multiplier |
|--------|--------|-----------|
| **Resolution** | `1k` | 1x |
| | `2k` | 1x |
| | `4k` | 1.8x |

<Tip>
**Default cost:** 1k resolution = 134,000 × 1 = **134,000 micro-cents ($0.134)**
</Tip>

## Response

<ResponseField name="jobId" type="string">
  Unique identifier for the submitted job.
</ResponseField>

<ResponseField name="status" type="string">
  Initial job status. Always `"pending"` on successful submission.
</ResponseField>

<ResponseField name="estimatedCompletionTime" type="string">
  ISO 8601 timestamp of the estimated completion time.
</ResponseField>

<ResponseField name="costMicroCents" type="number">
  The cost of the job in micro-cents.
</ResponseField>

## Code Examples

<CodeGroup>

```bash cURL
curl -X POST https://api.muvi.video/v1/jobs/submit \
  -H "Authorization: Bearer $PIXELBYTE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "google/nano-banana-pro-extra/edit-image",
    "input": {
      "prompt": "Replace the background with a snowy forest at dusk",
      "image_urls": ["https://example.com/image.jpg"],
      "aspect_ratio": "16:9",
      "resolution": "2k"
    }
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.muvi.video/v1/jobs/submit",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json"
    },
    json={
        "model": "google/nano-banana-pro-extra/edit-image",
        "input": {
            "prompt": "Replace the background with a snowy forest at dusk",
            "image_urls": ["https://example.com/image.jpg"],
            "aspect_ratio": "16:9",
            "resolution": "2k"
        }
    }
)

data = response.json()
print(data)
```

```javascript JavaScript
const response = await fetch("https://api.muvi.video/v1/jobs/submit", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    model: "google/nano-banana-pro-extra/edit-image",
    input: {
      prompt: "Replace the background with a snowy forest at dusk",
      image_urls: ["https://example.com/image.jpg"],
      aspect_ratio: "16:9",
      resolution: "2k"
    }
  })
});

const data = await response.json();
console.log(data);
```

</CodeGroup>
